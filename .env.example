# Supabase Configuration
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Keep false in production until the staff-session function has passed clone testing.
VITE_USE_SERVER_SESSION=false

# The browser only invokes an authenticated Edge Function for HN lookup.
# Keep this as search-hn in normal operation. Use search-hn-recovery only as
# the rehearsed rollback route; it must be deployed before it is selected.
VITE_HN_LOOKUP_FUNCTION=search-hn

# วิธีหาค่า:
# 1. เข้าไปที่ Supabase Dashboard -> Project Settings -> API
# 2. คัดลอก Project URL และ anon public key
# 3. เปลี่ยนชื่อไฟล์ .env.example เป็น .env
# 4. ใส่ค่าจริงของคุณ
